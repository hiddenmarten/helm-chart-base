suite: ServiceAccount template
templates:
  - templates/service_account.tpl

tests:
  - it: renders a ServiceAccount with name from values
    skip:
      reason: "Temporarily disabled while fixing template"
    set:
      serviceAccount:
        name: custom-sa
    asserts:
      - isKind:
          of: ServiceAccount
      - equal:
          path: metadata.name
          value: custom-sa
